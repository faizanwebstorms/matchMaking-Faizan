const getStatuses = [0, 1];
const getTypes = [0, 1, 2, 3];
const idTypes = ['standardId', 'facebookId', 'googleId', 'appleId'];

const status = {
  pending: 0,
  active: 1,
};

const types = {
  STANDARD: 0,
  FACEBOOK: 1,
  GOOGLE: 2,
  APPLE: 3,
};



module.exports = {
  getStatuses,
  getTypes,
  status,
  types,
  idTypes,

};
